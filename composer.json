{
    "name": "jeyroik/i-have-attributes",
    "description": "Simple attributes implementation",
    "require": {},
    "require-dev": {
        "phpunit/phpunit": "^9",
        "phpstan/phpstan": "0.*"
    },
    "autoload": {
        "psr-4": {
            "jeyroik\\": "src/"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit --testdox --coverage-clover=coverage.xml --whitelist=src tests",
        "stan": "vendor/bin/phpstan analyse -l 5 src"
    }
}